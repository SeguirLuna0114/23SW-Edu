import pandas as pd
import json
import matplotlib.pyplot as plt

plt.rcParams['font.family'] = 'Malgun Gothic'

with open(json_file, 'r', encoding='utf-8') as file:
    dataUlfptcaAlarms = json.loads(file)  ##json.loads()함수: file을 json으로 변환
myframe_Ulfptca = pd.DataFrame(dataUlfptcaAlarms)
#myframe_Ulfptca = pd.read_json(json_file) ##pd.read_json 이용하여 데이터프레임
#display(myframe_Ulfptca)

#PM25(초미세먼지) 데이터
# itemCode가 'PM25'인 항목 중 'issueVal' 데이터 가져오기
issue_vals_PM25 = []
issue_vals_PM10 = []

for item in data2018:
    if item['itemCode'] == 'PM25':
        issue_vals_2018_PM25.append(int(item['issueVal']))
    elif item['itemCode'] == 'PM10':
        issue_vals_2018_PM10.append(int(item['issueVal']))

myframe_Ulfptca.plot(kind='bar', rot=0, title='지역별 초미세먼지 지수', legend=True)


