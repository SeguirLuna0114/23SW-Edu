name = input("input name : ")
age = int(input("input age : ")) #입력값을 정수 변환

print("name : %s" % (name)) #name이 문자열이기에 format string %s으로 표기. 1개의 name변수이기에 ()생략 가능
print("age : %d" % (age)) #age가 정수이기에 %d로 표기. 1개의 age변수이기에 ()생략 가능
